---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

// Sample sermon data - in a real application, this would come from a CMS or database
const sermons = [
  {
    id: 1,
    title: "Walking in God's Purpose",
    speaker: "Pastor John Doe",
    date: "2024-12-15",
    series: "Discovering Your Destiny",
    topic: "Purpose",
    duration: "45 min",
    description: "Discover how to align your life with God's divine purpose and calling. Learn practical steps to discern God's will for your life.",
    scripture: "Jeremiah 29:11",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/8468730/pexels-photo-8468730.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: true,
    views: 245
  },
  {
    id: 2,
    title: "The Power of Faith",
    speaker: "Pastor Sarah Johnson",
    date: "2024-12-08",
    series: "Faith Foundations",
    topic: "Faith",
    duration: "38 min",
    description: "Explore how faith can move mountains and transform your circumstances. Understanding faith as a lifestyle, not just a feeling.",
    scripture: "Hebrews 11:1",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/5206040/pexels-photo-5206040.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: true,
    views: 189
  },
  {
    id: 3,
    title: "Love in Action",
    speaker: "Elder Michael Asante",
    date: "2024-12-01",
    series: "Living Like Jesus",
    topic: "Love",
    duration: "42 min",
    description: "Learn how to demonstrate Christ's love in practical ways. Moving beyond words to actions that transform communities.",
    scripture: "1 John 3:18",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/7551659/pexels-photo-7551659.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: false,
    views: 156
  },
  {
    id: 4,
    title: "Overcoming Life's Challenges",
    speaker: "Pastor John Doe",
    date: "2024-11-24",
    series: "Victory in Christ",
    topic: "Overcoming",
    duration: "50 min",
    description: "Biblical strategies for facing and overcoming the challenges that life throws at us. Finding strength in God's promises.",
    scripture: "Romans 8:37",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/8468722/pexels-photo-8468722.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: false,
    views: 203
  },
  {
    id: 5,
    title: "The Heart of Worship",
    speaker: "Pastor Sarah Johnson",
    date: "2024-11-17",
    series: "True Worship",
    topic: "Worship",
    duration: "35 min",
    description: "Understanding what true worship means and how to cultivate a heart that honors God in all circumstances.",
    scripture: "John 4:23-24",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/7551426/pexels-photo-7551426.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: false,
    views: 178
  },
  {
    id: 6,
    title: "Building Strong Relationships",
    speaker: "Elder Michael Asante",
    date: "2024-11-10",
    series: "Community Life",
    topic: "Relationships",
    duration: "44 min",
    description: "Biblical principles for building and maintaining healthy relationships in marriage, family, and friendship.",
    scripture: "Ecclesiastes 4:12",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/1024993/pexels-photo-1024993.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: false,
    views: 134
  },
  {
    id: 7,
    title: "Generosity and Blessing",
    speaker: "Pastor John Doe",
    date: "2024-11-03",
    series: "Kingdom Principles",
    topic: "Giving",
    duration: "41 min",
    description: "Discover the joy and blessing that comes from a generous heart. Understanding biblical stewardship and giving.",
    scripture: "2 Corinthians 9:7",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/6646918/pexels-photo-6646918.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: false,
    views: 167
  },
  {
    id: 8,
    title: "Prayer That Changes Things",
    speaker: "Pastor Sarah Johnson",
    date: "2024-10-27",
    series: "Power of Prayer",
    topic: "Prayer",
    duration: "39 min",
    description: "Learn how to pray effectively and see God move in powerful ways. Understanding different types of prayer and their purposes.",
    scripture: "James 5:16",
    audioUrl: "#",
    videoUrl: "#",
    notesUrl: "#",
    thumbnail: "https://images.pexels.com/photos/8468730/pexels-photo-8468730.jpeg?auto=compress&cs=tinysrgb&w=800",
    featured: false,
    views: 221
  }
];

const sermonSeries = [
  { name: "All Series", value: "all" },
  { name: "Discovering Your Destiny", value: "discovering-your-destiny" },
  { name: "Faith Foundations", value: "faith-foundations" },
  { name: "Living Like Jesus", value: "living-like-jesus" },
  { name: "Victory in Christ", value: "victory-in-christ" },
  { name: "True Worship", value: "true-worship" },
  { name: "Community Life", value: "community-life" },
  { name: "Kingdom Principles", value: "kingdom-principles" },
  { name: "Power of Prayer", value: "power-of-prayer" }
];

const sermonTopics = [
  { name: "All Topics", value: "all" },
  { name: "Purpose", value: "purpose" },
  { name: "Faith", value: "faith" },
  { name: "Love", value: "love" },
  { name: "Overcoming", value: "overcoming" },
  { name: "Worship", value: "worship" },
  { name: "Relationships", value: "relationships" },
  { name: "Giving", value: "giving" },
  { name: "Prayer", value: "prayer" }
];

// Helper function to format date
function formatDate(dateString: string) {
  const date = new Date(dateString);
  return date.toLocaleDateString('en-US', { 
    year: 'numeric', 
    month: 'long', 
    day: 'numeric' 
  });
}

// Helper function to get time ago
function getTimeAgo(dateString: string) {
  const date = new Date(dateString);
  const now = new Date();
  const diffTime = Math.abs(now.getTime() - date.getTime());
  const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
  
  if (diffDays === 1) return '1 day ago';
  if (diffDays < 7) return `${diffDays} days ago`;
  if (diffDays < 14) return '1 week ago';
  if (diffDays < 30) return `${Math.floor(diffDays / 7)} weeks ago`;
  if (diffDays < 60) return '1 month ago';
  return `${Math.floor(diffDays / 30)} months ago`;
}
---

<Layout 
  title="Sermons & Messages | Omet Omeni Ministries - Spiritual Growth & Teaching" 
  description="Listen to inspiring sermons and biblical teachings from Omet Omeni Ministries in Kumasi, Ghana. Access audio, video, and sermon notes to grow in your faith journey."
>
  <Header />
  
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-primary-50 via-white to-secondary-50 py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center">
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-display font-bold text-neutral-900 mb-6">
          Sermons & <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 to-secondary-600">Messages</span>
        </h1>
        <p class="text-xl text-neutral-600 max-w-3xl mx-auto mb-8">
          Grow in your faith with inspiring biblical teachings, practical life applications, and powerful messages from our pastoral team.
        </p>
        
        <!-- Quick Stats -->
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 max-w-2xl mx-auto">
          <div class="bg-white p-4 rounded-lg shadow-md border border-neutral-100">
            <div class="text-2xl font-bold text-primary-600">{sermons.length}+</div>
            <div class="text-neutral-600 text-sm">Sermons Available</div>
          </div>
          <div class="bg-white p-4 rounded-lg shadow-md border border-neutral-100">
            <div class="text-2xl font-bold text-secondary-600">{sermonSeries.length - 1}</div>
            <div class="text-neutral-600 text-sm">Sermon Series</div>
          </div>
          <div class="bg-white p-4 rounded-lg shadow-md border border-neutral-100">
            <div class="text-2xl font-bold text-primary-600">3</div>
            <div class="text-neutral-600 text-sm">Teaching Pastors</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Sermons -->
  <section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl sm:text-4xl font-display font-bold text-neutral-900 mb-4">
          Featured Messages
        </h2>
        <p class="text-xl text-neutral-600">
          Don't miss these powerful and life-changing messages from our recent services
        </p>
      </div>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {sermons.filter(sermon => sermon.featured).map((sermon) => (
          <div class="bg-white rounded-2xl shadow-xl overflow-hidden hover:shadow-2xl transition-shadow duration-300 border border-neutral-100">
            <div class="relative">
              <img 
                src={sermon.thumbnail} 
                alt={sermon.title}
                class="w-full h-64 object-cover"
              />
              <div class="absolute inset-0 bg-black/40 flex items-center justify-center opacity-0 hover:opacity-100 transition-opacity duration-300">
                <div class="flex space-x-4">
                  <button class="w-12 h-12 bg-white/90 rounded-full flex items-center justify-center hover:bg-white transition-colors">
                    <svg class="w-6 h-6 text-primary-600" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M8 5v14l11-7z"/>
                    </svg>
                  </button>
                  <button class="w-12 h-12 bg-white/90 rounded-full flex items-center justify-center hover:bg-white transition-colors">
                    <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.536 8.464a5 5 0 010 7.072m2.828-9.9a9 9 0 010 14.142M6.343 6.343a9 9 0 000 12.728m2.829-9.9a5 5 0 000 7.072M12 12h.01"></path>
                    </svg>
                  </button>
                </div>
              </div>
              <div class="absolute top-4 left-4">
                <span class="bg-primary-600 text-white px-3 py-1 rounded-full text-sm font-medium">
                  Featured
                </span>
              </div>
              <div class="absolute top-4 right-4 bg-white/90 backdrop-blur-sm rounded-lg px-2 py-1">
                <span class="text-neutral-800 text-sm font-medium">{sermon.duration}</span>
              </div>
            </div>
            
            <div class="p-8">
              <div class="flex items-center text-sm text-neutral-500 mb-3">
                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
                {formatDate(sermon.date)} • {getTimeAgo(sermon.date)}
              </div>
              
              <h3 class="text-2xl font-bold text-neutral-900 mb-3">{sermon.title}</h3>
              
              <div class="flex items-center mb-4">
                <div class="w-8 h-8 bg-gradient-to-br from-primary-500 to-primary-600 rounded-full flex items-center justify-center mr-3">
                  <span class="text-white text-sm font-semibold">{sermon.speaker.split(' ').map(n => n[0]).join('')}</span>
                </div>
                <div>
                  <p class="font-medium text-neutral-900">{sermon.speaker}</p>
                  <p class="text-sm text-neutral-600">{sermon.series}</p>
                </div>
              </div>
              
              <p class="text-neutral-600 mb-4 leading-relaxed">{sermon.description}</p>
              
              <div class="flex items-center justify-between mb-6">
                <div class="flex items-center text-sm text-neutral-500">
                  <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                  </svg>
                  {sermon.scripture}
                </div>
                <div class="flex items-center text-sm text-neutral-500">
                  <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                  </svg>
                  {sermon.views} views
                </div>
              </div>
              
              <div class="flex flex-wrap gap-3">
                <button class="flex items-center px-4 py-2 bg-primary-600 text-white rounded-lg hover:bg-primary-700 transition-colors">
                  <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                  Watch Video
                </button>
                <button class="flex items-center px-4 py-2 bg-secondary-600 text-white rounded-lg hover:bg-secondary-700 transition-colors">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.536 8.464a5 5 0 010 7.072m2.828-9.9a9 9 0 010 14.142M6.343 6.343a9 9 0 000 12.728m2.829-9.9a5 5 0 000 7.072M12 12h.01"></path>
                  </svg>
                  Listen Audio
                </button>
                <button class="flex items-center px-4 py-2 bg-white text-neutral-700 rounded-lg border border-neutral-200 hover:bg-neutral-50 transition-colors">
                  <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                  </svg>
                  Notes
                </button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Filter Section -->
  <section class="py-20 bg-neutral-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-12">
        <h2 class="text-3xl sm:text-4xl font-display font-bold text-neutral-900 mb-4">
          All Sermons
        </h2>
        <p class="text-xl text-neutral-600 mb-8">
          Browse our complete library of sermons and find messages that speak to your heart
        </p>
        
        <!-- Search Bar -->
        <div class="max-w-md mx-auto mb-8">
          <div class="relative">
            <input 
              type="text" 
              id="sermon-search"
              placeholder="Search sermons, speakers, or topics..."
              class="w-full px-4 py-3 pl-12 border border-neutral-200 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
            />
            <svg class="absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 text-neutral-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
          </div>
        </div>
        
        <!-- Filter Buttons -->
        <div class="flex flex-wrap justify-center gap-4 mb-8">
          <div class="flex flex-wrap gap-2">
            <span class="text-sm font-medium text-neutral-700 px-3 py-2">Series:</span>
            {sermonSeries.map((series) => (
              <button 
                class="px-3 py-2 text-sm font-medium rounded-full transition-colors series-filter bg-primary-100 text-primary-700 hover:bg-primary-200"
                data-series={series.value}
              >
                {series.name}
              </button>
            ))}
          </div>
        </div>
        
        <div class="flex flex-wrap justify-center gap-4">
          <div class="flex flex-wrap gap-2">
            <span class="text-sm font-medium text-neutral-700 px-3 py-2">Topics:</span>
            {sermonTopics.map((topic) => (
              <button 
                class="px-3 py-2 text-sm font-medium rounded-full transition-colors topic-filter bg-secondary-100 text-secondary-700 hover:bg-secondary-200"
                data-topic={topic.value}
              >
                {topic.name}
              </button>
            ))}
          </div>
        </div>
      </div>
      
      <!-- Sermons Grid -->
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8" id="sermons-grid">
        {sermons.map((sermon) => (
          <div 
            class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300 sermon-card"
            data-series={sermon.series.toLowerCase().replace(/\s+/g, '-').replace(/'/g, '')}
            data-topic={sermon.topic.toLowerCase()}
            data-speaker={sermon.speaker.toLowerCase()}
            data-title={sermon.title.toLowerCase()}
          >
            <div class="relative h-48">
              <img 
                src={sermon.thumbnail} 
                alt={sermon.title}
                class="w-full h-full object-cover"
              />
              <div class="absolute inset-0 bg-black/40 flex items-center justify-center opacity-0 hover:opacity-100 transition-opacity duration-300">
                <button class="w-12 h-12 bg-white/90 rounded-full flex items-center justify-center hover:bg-white transition-colors">
                  <svg class="w-6 h-6 text-primary-600" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M8 5v14l11-7z"/>
                  </svg>
                </button>
              </div>
              <div class="absolute top-3 left-3">
                <span class="bg-white/90 backdrop-blur-sm text-neutral-800 px-2 py-1 rounded-full text-xs font-medium">
                  {sermon.topic}
                </span>
              </div>
              <div class="absolute top-3 right-3 bg-black/60 text-white rounded px-2 py-1">
                <span class="text-xs font-medium">{sermon.duration}</span>
              </div>
            </div>
            
            <div class="p-6">
              <div class="flex items-center text-xs text-neutral-500 mb-2">
                <span>{formatDate(sermon.date)}</span>
                <span class="mx-2">•</span>
                <span>{sermon.views} views</span>
              </div>
              
              <h3 class="text-lg font-semibold text-neutral-900 mb-2 line-clamp-2">{sermon.title}</h3>
              
              <div class="flex items-center mb-3">
                <div class="w-6 h-6 bg-gradient-to-br from-primary-500 to-primary-600 rounded-full flex items-center justify-center mr-2">
                  <span class="text-white text-xs font-semibold">{sermon.speaker.split(' ').map(n => n[0]).join('')}</span>
                </div>
                <div>
                  <p class="text-sm font-medium text-neutral-900">{sermon.speaker}</p>
                </div>
              </div>
              
              <p class="text-neutral-600 text-sm mb-3 line-clamp-2">{sermon.description}</p>
              
              <div class="flex items-center justify-between text-xs text-neutral-500 mb-4">
                <span class="bg-neutral-100 px-2 py-1 rounded">{sermon.series}</span>
                <span>{sermon.scripture}</span>
              </div>
              
              <div class="flex space-x-2">
                <button class="flex-1 bg-primary-600 text-white py-2 px-3 rounded text-sm hover:bg-primary-700 transition-colors">
                  Watch
                </button>
                <button class="flex-1 bg-secondary-600 text-white py-2 px-3 rounded text-sm hover:bg-secondary-700 transition-colors">
                  Listen
                </button>
                <button class="bg-neutral-200 text-neutral-700 py-2 px-3 rounded text-sm hover:bg-neutral-300 transition-colors">
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                  </svg>
                </button>
              </div>
            </div>
          </div>
        ))}
      </div>
      
      <!-- Load More Button -->
      <div class="text-center mt-12">
        <button class="bg-primary-600 text-white px-8 py-3 rounded-lg hover:bg-primary-700 transition-colors font-medium">
          Load More Sermons
        </button>
      </div>
    </div>
  </section>

  <!-- Sermon Series Spotlight -->
  <section class="py-20 bg-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-3xl sm:text-4xl font-display font-bold text-neutral-900 mb-4">
          Current Series
        </h2>
        <p class="text-xl text-neutral-600">
          Join us as we journey through our current sermon series
        </p>
      </div>
      
      <div class="bg-gradient-to-br from-primary-50 to-secondary-50 rounded-2xl p-8 lg:p-12">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-center">
          <div>
            <div class="inline-block bg-primary-600 text-white px-4 py-2 rounded-full text-sm font-medium mb-4">
              Current Series
            </div>
            <h3 class="text-3xl font-display font-bold text-neutral-900 mb-4">
              Discovering Your Destiny
            </h3>
            <p class="text-lg text-neutral-600 mb-6 leading-relaxed">
              A powerful 6-week series exploring God's unique purpose and calling for your life. Learn how to align your dreams with God's plan and step boldly into your destiny.
            </p>
            
            <div class="space-y-3 mb-8">
              <div class="flex items-center text-neutral-700">
                <svg class="w-5 h-5 text-primary-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
                <span><strong>Duration:</strong> 6 weeks (November - December 2024)</span>
              </div>
              <div class="flex items-center text-neutral-700">
                <svg class="w-5 h-5 text-primary-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                </svg>
                <span><strong>Speaker:</strong> Pastor John Doe</span>
              </div>
              <div class="flex items-center text-neutral-700">
                <svg class="w-5 h-5 text-primary-600 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v10a2 2 0 002 2h8a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"></path>
                </svg>
                <span><strong>Progress:</strong> Week 4 of 6</span>
              </div>
            </div>
            
            <div class="flex flex-col sm:flex-row gap-4">
              <button class="bg-primary-600 text-white px-6 py-3 rounded-lg hover:bg-primary-700 transition-colors font-medium">
                View All Episodes
              </button>
              <button class="bg-white text-primary-600 px-6 py-3 rounded-lg border-2 border-primary-200 hover:bg-primary-50 transition-colors font-medium">
                Download Study Guide
              </button>
            </div>
          </div>
          
          <div class="relative">
            <img 
              src="https://images.pexels.com/photos/8468730/pexels-photo-8468730.jpeg?auto=compress&cs=tinysrgb&w=800" 
              alt="Discovering Your Destiny series"
              class="w-full h-80 object-cover rounded-xl shadow-lg"
            />
            <div class="absolute inset-0 bg-black/40 rounded-xl flex items-center justify-center">
              <button class="w-16 h-16 bg-white/90 rounded-full flex items-center justify-center hover:bg-white transition-colors">
                <svg class="w-8 h-8 text-primary-600" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M8 5v14l11-7z"/>
                </svg>
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Podcast & Subscription -->
  <section class="py-20 bg-gradient-to-br from-primary-600 to-secondary-600 text-white">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="space-y-8">
        <div>
          <h2 class="text-3xl sm:text-4xl font-display font-bold mb-4">
            Never Miss a Message
          </h2>
          <p class="text-xl opacity-90">
            Subscribe to our podcast and get notified when new sermons are available
          </p>
        </div>
        
        <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-8 border border-white/20">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-8 items-center">
            <div class="text-left">
              <h3 class="text-2xl font-semibold mb-4">Omet Omeni Ministries Podcast</h3>
              <ul class="space-y-3 opacity-90">
                <li class="flex items-center">
                  <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                  </svg>
                  Weekly sermon uploads
                </li>
                <li class="flex items-center">
                  <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                  </svg>
                  High-quality audio
                </li>
                <li class="flex items-center">
                  <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                  </svg>
                  Available on all platforms
                </li>
                <li class="flex items-center">
                  <svg class="w-5 h-5 mr-3" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"></path>
                  </svg>
                  Free to subscribe
                </li>
              </ul>
            </div>
            
            <div class="space-y-4">
              <h4 class="text-lg font-semibold">Subscribe on:</h4>
              <div class="grid grid-cols-2 gap-3">
                <a href="#" class="bg-white/20 hover:bg-white/30 transition-colors rounded-lg p-3 flex items-center justify-center">
                  <span class="font-medium">Apple Podcasts</span>
                </a>
                <a href="#" class="bg-white/20 hover:bg-white/30 transition-colors rounded-lg p-3 flex items-center justify-center">
                  <span class="font-medium">Spotify</span>
                </a>
                <a href="#" class="bg-white/20 hover:bg-white/30 transition-colors rounded-lg p-3 flex items-center justify-center">
                  <span class="font-medium">Google Podcasts</span>
                </a>
                <a href="https://www.youtube.com/@oominternational" target="_blank" rel="noopener noreferrer" class="bg-white/20 hover:bg-white/30 transition-colors rounded-lg p-3 flex items-center justify-center">
                  <span class="font-medium">YouTube</span>
                </a>
              </div>
            </div>
          </div>
        </div>
        
        <!-- Newsletter Signup -->
        <div class="bg-white rounded-2xl p-8 text-neutral-900">
          <h3 class="text-2xl font-semibold mb-4">Get Sermon Notifications</h3>
          <p class="text-neutral-600 mb-6">
            Subscribe to receive email notifications when new sermons are available
          </p>
          <form class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
            <input 
              type="email" 
              placeholder="Enter your email address"
              class="flex-1 px-4 py-3 border border-neutral-200 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
            />
            <button 
              type="submit"
              class="bg-primary-600 text-white px-6 py-3 rounded-lg hover:bg-primary-700 transition-colors font-medium"
            >
              Subscribe
            </button>
          </form>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>

<script>
  // Sermon filtering and search functionality
  document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('sermon-search');
    const seriesFilters = document.querySelectorAll('.series-filter');
    const topicFilters = document.querySelectorAll('.topic-filter');
    const sermonCards = document.querySelectorAll('.sermon-card');
    
    let currentSeries = 'all';
    let currentTopic = 'all';
    let currentSearch = '';
    
    // Search functionality
    searchInput?.addEventListener('input', function() {
      currentSearch = this.value.toLowerCase();
      filterSermons();
    });
    
    // Series filter functionality
    seriesFilters.forEach(filter => {
      filter.addEventListener('click', function() {
        // Update active state
        seriesFilters.forEach(f => {
          f.classList.remove('bg-primary-600', 'text-white');
          f.classList.add('bg-primary-100', 'text-primary-700');
        });
        this.classList.remove('bg-primary-100', 'text-primary-700');
        this.classList.add('bg-primary-600', 'text-white');
        
        currentSeries = this.getAttribute('data-series');
        filterSermons();
      });
    });
    
    // Topic filter functionality
    topicFilters.forEach(filter => {
      filter.addEventListener('click', function() {
        // Update active state
        topicFilters.forEach(f => {
          f.classList.remove('bg-secondary-600', 'text-white');
          f.classList.add('bg-secondary-100', 'text-secondary-700');
        });
        this.classList.remove('bg-secondary-100', 'text-secondary-700');
        this.classList.add('bg-secondary-600', 'text-white');
        
        currentTopic = this.getAttribute('data-topic');
        filterSermons();
      });
    });
    
    // Filter sermons based on current filters
    function filterSermons() {
      sermonCards.forEach(card => {
        const series = card.getAttribute('data-series');
        const topic = card.getAttribute('data-topic');
        const speaker = card.getAttribute('data-speaker');
        const title = card.getAttribute('data-title');
        
        const matchesSeries = currentSeries === 'all' || series === currentSeries;
        const matchesTopic = currentTopic === 'all' || topic === currentTopic;
        const matchesSearch = currentSearch === '' || 
          title.includes(currentSearch) || 
          speaker.includes(currentSearch) || 
          topic.includes(currentSearch);
        
        if (matchesSeries && matchesTopic && matchesSearch) {
          card.style.display = 'block';
          card.classList.add('animate-fade-in');
        } else {
          card.style.display = 'none';
        }
      });
    }
    
    // Newsletter subscription
    const newsletterForm = document.querySelector('form');
    newsletterForm?.addEventListener('submit', function(e) {
      e.preventDefault();
      
      const button = this.querySelector('button[type="submit"]');
      const originalText = button.textContent;
      
      button.textContent = 'Subscribing...';
      button.disabled = true;
      
      // Simulate subscription
      setTimeout(() => {
        button.textContent = 'Subscribed!';
        button.classList.remove('bg-primary-600', 'hover:bg-primary-700');
        button.classList.add('bg-green-600');
        
        // Reset form
        this.reset();
        
        // Reset button after 3 seconds
        setTimeout(() => {
          button.textContent = originalText;
          button.disabled = false;
          button.classList.remove('bg-green-600');
          button.classList.add('bg-primary-600', 'hover:bg-primary-700');
        }, 3000);
      }, 2000);
    });
    
    // Video/Audio player simulation
    const playButtons = document.querySelectorAll('button:has(svg path[d="M8 5v14l11-7z"])');
    playButtons.forEach(button => {
      button.addEventListener('click', function() {
        // In a real application, this would open a video/audio player
        alert('Video/Audio player would open here. Integration with video hosting service needed.');
      });
    });
  });
</script>

<style>
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .animate-fade-in {
    animation: fadeIn 0.5s ease-in-out;
  }
  
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
  }
</style>
